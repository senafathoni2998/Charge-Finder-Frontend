{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": false
  },
  "include": ["src"],
  "//": "Test files are excluded from the typecheck gate for now (they have pre-existing type gaps — missing vitest globals, fixture types). Re-include them when strict mode + typed test fixtures land (refactor plan RF4).",
  "exclude": ["**/*.test.ts", "**/*.test.tsx", "src/setupTests.ts"]
}
